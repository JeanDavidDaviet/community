<form method="post" action="{{ path('page_group_delete', {'id': page_group.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ page_group.id) }}">
    <button class="btn">Delete</button>
</form>
