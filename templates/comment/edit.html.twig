{% extends 'base.html.twig' %}

{% block title %}{{ "Éditer le commentaire" | trans }}{% endblock %}

{% block body %}
    <h1>{{ "Éditer le commentaire" | trans }}</h1>

    {{ include('comment/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('comment_index') }}">{{ "Retour à la liste" | trans }}</a>

    {{ include('comment/_delete_form.html.twig') }}
{% endblock %}
